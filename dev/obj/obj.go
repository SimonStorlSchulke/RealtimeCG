package obj

/*
//Returns a array of floats (gl verts) from a .obj file
func LoadObj(path string) ([]float32, error) {
	f, err := ioutil.ReadFile(path)
	if err != nil {
		return nil, err
	}
	fStr := string(f[:])
	return
}
*/
